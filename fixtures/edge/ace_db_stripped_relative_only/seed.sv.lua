QUI_DB = {
    profileKeys = { ["TestChar - TestRealm"] = "Default" },
    profiles = {
        Default = {
            _schemaVersion = 24,  -- v25's input
            frameAnchoring = {
                debuffFrame = {
                    parent = "disabled",
                    -- point absent — stripped by AceDB because it matched the default
                    relative = "TOPRIGHT",
                    x = -150,
                    y = -200,
                },
            },
        },
    },
}
QUIDB = {}

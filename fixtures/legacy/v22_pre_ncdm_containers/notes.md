# v22_pre_ncdm_containers

A profile at _schemaVersion = 21, before MigrateNCDMContainers.
expected.sv.lua should show the post-v22 shape with ncdm.containers
populated and _schemaVersion bumped to current (32).

Reference: core/migrations.lua v22 section.
